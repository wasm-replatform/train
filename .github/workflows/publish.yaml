name: Publish Release

on:
  workflow_dispatch:

jobs:
  ci:
    name: CI
    uses: credibil/pipeline/.github/workflows/ci.yaml@main
    secrets:
      CARGO_REGISTRIES_CREDIBIL_TOKEN: ${{ secrets.CARGO_REGISTRIES_CREDIBIL_TOKEN }}

  publish:
    name: Publish
    needs: ci
    uses: credibil/pipeline/.github/workflows/publish.yaml@main

  deploy:
    name: Deploy
    needs: publish
    uses: credibil/pipeline/.github/workflows/wasm.yaml@main
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    with:
      package: r9k_position
      storage-account: stcredibildemo
      resource-group: rg-credibil-demo
