# Realtime utilities

Shared traits, error types, and abstractions consumed by the WASM guests and domain crates. These definitions provide the trait contracts (Config, HttpRequest, Publisher, StateStore, Identity) used for dependency injection across the adapters.
