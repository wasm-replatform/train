# Realtime utilities

Shared traits, error types, and abstractions consumed by the WASM guests and domain crates. These definitions provide the Provider contracts used for dependency injection across the adapters.
